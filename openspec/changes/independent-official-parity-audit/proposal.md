## 为什么现在必须重做

此前的测试通过和零散对照，不能证明官方实际会走到的每个规则判断都已检查。执行过程中仍能发现数条差异，说明原计划按大类检查的粒度不够。要给出“代码级 100% 复刻”的结论，必须先把固定官方版本里所有会改变对局结果的分支逐条列清，再一次性修完已确认差异，最后以完整证据关单。

## 这次要做什么

- 以 `work/upstream-official` 的 `ca547ccb0ed01e6f282f9e8e71f7a24d547b24d7` 为唯一玩法基准，从真正的开局和对局入口向下追踪，不把旧注释、未调用代码或网页管理功能当成规则。
- 建立完整的“官方分支总表”。总表的每一行必须包含：官方文件和行号、触发条件、官方结果、Python 对应位置、最小测试、实际结果、状态和差异编号。
- 对角色分配、所有角色、昼夜阶段、行动冲突、死亡链、转换、投票、胜负、设置、保存恢复和 QQ 文字入口逐条核对。不能用“同类角色已测”代替具体角色或具体判断。
- 在开始改动前冻结全部已发现差异。每一条差异先有稳定的最小测试，再集中修改相关代码；不得把一次运行中偶然发现的问题留到以后。
- 只有分支总表的“未检查”和“未决”均为零、所有差异均已修复且完整测试通过，才允许标记代码级 `100%`。
- Telegram 按钮、动图、语言包、成就和 QQ 菜单排版只作为展示差异记录；QQ 文字指令能否进入相同规则流程仍属于本次检查。

## 新增能力

- `independent-official-parity-audit`：以官方实际调用路径为准，建立可计数、可复查、不可跳项的完整玩法对照和结论。
- `official-parity-remediation`：在完整差异清单冻结后，集中修复所有确认差异，并提供测试结果和可回退材料。

## 影响范围

- 核心对照来源：`Werewolf Node/Werewolf.cs`、`Shared/Roles.cs`、`Shared/GameBalancing.cs`、`Shared/GameMode.cs`、相关模型与设置文件，以及官方命令入口。
- 可能修改：`werewolf-python/domain/`、`application/`、`adapters/qq/`、`infrastructure/`、`tests/` 和现有对照文档；只修改被官方总表确认有差异的部分。
- 不修改官方 Telegram 源码，不把管理网站、捐赠、统计或展示素材当成游戏玩法，也不在本次工作中代替用户进行真实 QQ 对局。

## 完成标准

本次工作不是“再多加一些测试”。完成时必须同时满足：

1. 官方分支总表覆盖全部实际调用的玩法判断，并有总数与状态统计。
2. 每一行都有来源、Python 对应处、固定输入和实际结果；没有“凭人工目测一致”的行。
3. 已发现差异全部有编号、最小测试、修复记录和复查结果。
4. 官方对照测试、QQ 指令测试、保存恢复测试和全量测试全部通过。
5. 未检查数、未决数、失败数均为零；否则结论只能写“尚未完成”，不能写 `100%`。
