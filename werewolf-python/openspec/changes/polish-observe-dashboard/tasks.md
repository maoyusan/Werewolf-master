## 1. 令牌与页面骨架

- [x] 1.1 在 `DASHBOARD_HTML` 的 `:root` 写入设计令牌（底、卡片、主文字、次级、边框、正常、警告、错误、焦点环、八像素间距阶），组件选择器改为引用变量；目视源码中业务色不再散落裸十六进制
- [x] 1.2 把页面改成左右栏应用壳：左侧品牌「狼人杀 · 运行检测台」与分区导航，右侧粘性顶栏（一级标题、单一连接态、快照时间）加健康条、指标网格、对局主区、底部分区；打开 `/dashboard` 能看到侧栏与上述区域
- [x] 1.3 指标改为卡片网格（对局、玩家/存活、排队、等待锚点、失败、已判死），窄屏单列、宽屏自动折行；根层不出现整页横滚，宽表只在卡片内滚动

## 2. 状态语义与无障碍

- [x] 2.1 内联轮廓矢量图标覆盖正常、提示、警告、错误、等待；诊断条、网关、连接态同时带中文标签与图标，源码中不出现表情符号充当图标
- [x] 2.2 连接态容器加上 `role="status"` 与 `aria-atomic="true"`，文案为完整中文句子；指标数字变化不另开实时区
- [x] 2.3 观测错误写入顶部 `role="alert"` 摘要，首次出现可移焦，同一错误刷新不再反复夺焦；用构造错误列表的快照或手工断接口确认告警可见
- [x] 2.4 折叠改为带 `aria-expanded` 的按钮，标题层级从一级顺序向下；键盘可展开收起，焦点环可见，粘性顶栏配合 `scroll-padding-top`
- [x] 2.5 过渡只做颜色、透明度、阴影（一百五十到二百毫秒），并写 `prefers-reduced-motion: reduce` 归零；系统开启减少动态后无位移入场

## 3. 对局卡与空态

- [x] 3.1 对局卡保持「诊断条 → 键值 → 折叠明细」；玩家表默认展开，无数据的投票/夜间可默认收起，事件与异常投递默认展开
- [x] 3.2 无对局时对局区显示说明性空态，总览、日志、异常投递仍可见；首次加载有占位而不是空白主区
- [x] 3.3 确认页面无改局控件，实时与轮询路径仍只读刷新 `/api/observe` 与 `/ws/observe`；网关与失败数只读快照已有字段

## 4. 测试与文档

- [x] 4.1 新增检测页静态测试：请求 `/dashboard`（或读取 `DASHBOARD_HTML`）断言标题含「检测台」、存在一级标题、连接态带状态角色、折叠为按钮、源码不含外链字体或外链脚本主机
- [x] 4.2 运行该新测试及既有适配器相关用例到全绿，不改 `/healthz` 与 `/readyz` 契约
- [x] 4.3 更新 `README.md` 与 `docs/游玩教程.md` 中观测页描述，使其与「运行检测台」分区用语一致
